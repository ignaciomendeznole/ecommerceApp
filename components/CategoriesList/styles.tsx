import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginVertical: 2,
  },
  title: {
    color: "white",
    marginBottom: 10,
    marginLeft: 10,
    fontSize: 30,
    fontWeight: "bold",
  },
});

export default styles;
